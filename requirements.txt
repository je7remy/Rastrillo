# requirements.txt — RANGOS para instalación flexible.
# Para reproducibilidad bit-a-bit (CI, releases, pip-audit) usa
# `requirements.lock` (versiones fijas, regenerable con pip-compile).
playwright>=1.44
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
rich>=13.7
sherlock-project>=0.15.0
holehe>=1.61
anthropic>=0.30        # opcional: solo para el modo IA de respaldo
reportlab>=4.0         # generación de PDF para el informe
openpyxl>=3.1          # generación de XLSX para el informe (ya venía vía sherlock)
dnspython>=2.6         # DNS (MX/NS/TXT) del módulo Domain Intelligence
# maigret              # opcional — amplía cobertura del scan; pip install maigret para activar
